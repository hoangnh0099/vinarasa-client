/**
 * @format
 */

import React from 'react';
import LoginModalPresenter from './presenter';

const LoginModal = (props) => {
  return <LoginModalPresenter {...props} />;
};

export default LoginModal;
