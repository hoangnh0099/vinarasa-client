/**
 * @format
 */

import React from 'react';
import ProductCardView from './view';

const ProductCardPresenter = props => {
  return <ProductCardView {...props} />;
};

export default ProductCardPresenter;
