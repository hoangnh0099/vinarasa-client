/**
 * @format
 */

import React from 'react';
import ProductCardPresenter from './presenter';

const ProductCard = props => {
  return <ProductCardPresenter {...props} />;
};

export default ProductCard;
